package dev.manere.utils.storage.sql.enums;

/**
 * An enumeration representing whether a column is a primary key or not in a SQL database.
 */
public enum PrimaryColumn {
    /**
     * Represents that the column is not a primary key.
     */
    FALSE,

    /**
     * Represents that the column is a primary key.
     */
    TRUE
}
